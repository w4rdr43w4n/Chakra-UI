enum settings {
  toast_max_msg_count = 3,
  msgDurarion = 4000,
  sessionExpiracy = 3600000,
  tokenExpiracyMinutes = 10,
  tokenExpiracy = tokenExpiracyMinutes * 60 * 1000,
  origin_url = 'http://localhost:3000'
}

export default settings;
